rules_version = '2';

// This version stores compressed private images in protected Firestore records.
// Cloud Storage is intentionally unused and denied by default.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
